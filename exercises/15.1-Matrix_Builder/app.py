#Import random

#Create the function below:
def matrixBuilder(i):
    numb_of_matrix = 5
    matrix = [1] * numb_of_matrix
    for i in range(numb_of_matrix):
        matrix.append(i)
        return (i)
    print (matrix)